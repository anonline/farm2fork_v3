export type WPOrderItemMeta = {
    [key in WPOrderItemMetaKeys]: string | number | boolean | [] | object | null;
}

export enum WPOrderItemMetaKeys {
    _alg_wc_cog_item_cost = '_alg_wc_cog_item_cost',
    _alg_wc_cog_item_profit = '_alg_wc_cog_item_profit',
    _alg_wc_cog_item_profit_percent = '_alg_wc_cog_item_profit_percent',
    _backorders_allowed = '_backorders_allowed',
    _bundle_discount_amount = '_bundle_discount_amount',
    _bundle_items = '_bundle_items',
    _bundle_parent = '_bundle_parent',
    _cart_discount = '_cart_discount',
    _cart_discount_tax = '_cart_discount_tax',
    _composite_data = '_composite_data',
    _composite_id = '_composite_id',
    _composite_is_bundled = '_composite_is_bundled',
    _composite_parent = '_composite_parent',
    _composite_prorated_line_total = '_composite_prorated_line_total',
    _composite_prorated_line_total_tax = '_composite_prorated_line_total_tax',
    _downloadable_files = '_downloadable_files',
    _fee_amount = '_fee_amount',
    _isCustom = '_isCustom',
    _isCustomUnit = '_isCustomUnit',
    _last_unitprice = '_last_unitprice',
    _line_subtotal = '_line_subtotal',
    _line_subtotal_tax = '_line_subtotal_tax',
    _line_tax = '_line_tax',
    _line_tax_data = '_line_tax_data',
    _line_total = '_line_total',
    _original_qty = '_original_qty',
    _original_subtotal = '_original_subtotal',
    _original_tax = '_original_tax',
    _product_id = '_product_id',
    _qty = '_qty',
    _reduced_stock = '_reduced_stock',
    _tax_class = '_tax_class',
    _tax_status = '_tax_status',
    _unit = '_unit',
    _variation_id = '_variation_id',
    compound = 'compound',
    cost = 'cost',
    cquantity = 'cquantity',
    custom_note = 'custom_note',
    ename = 'ename',
    instance_id = 'instance_id',
    label = 'label',
    method_id = 'method_id',
    note = 'note',
    pickup_address = 'pickup_address',
    pickup_details = 'pickup_details',
    pickup_location = 'pickup_location',
    pickup_time = 'pickup_time',
    rate_id = 'rate_id',
    rate_percent = 'rate_percent',
    shipping_note = 'shipping_note',
    shipping_tax_amount = 'shipping_tax_amount',
    tax_amount = 'tax_amount',
    taxes = 'taxes',
    Tételek = 'Tételek',
    total_tax = 'total_tax'
}
